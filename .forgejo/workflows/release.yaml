name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Log in to Codeberg Container Registry
        uses: docker/login-action@v3
        with:
          registry: codeberg.org
          username: ${{ github.actor }}
          password: ${{ secrets.FORGEJO_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            codeberg.org/r5t/cert-manager-webhook-hostup:${{ steps.version.outputs.version }}
            codeberg.org/r5t/cert-manager-webhook-hostup:latest

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Package and push Helm chart
        run: |
          VERSION=${{ steps.version.outputs.version }}
          helm package helm/hostup-webhook \
            --version $VERSION \
            --app-version $VERSION \
            --destination _out/
          helm push _out/hostup-webhook-$VERSION.tgz oci://codeberg.org/r5t
